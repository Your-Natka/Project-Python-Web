# Project-Python-Web
Посилання на сторінку Trello:  https://trello.com/b/iF7xAdAh/project-python-web

Посилання на сторінку презентації:  

📸 PhotoShare — REST API на FastAPI
🧩 Опис проєкту

PhotoShare — це REST API-застосунок для обміну фотографіями, створений на основі FastAPI.
Користувачі можуть завантажувати світлини, додавати описи, теги, коментарі, виставляти рейтинг, а також переглядати зображення з урахуванням фільтрів і трансформацій через Cloudinary.

Проєкт підтримує аутентифікацію через JWT, різні ролі користувачів (звичайний, модератор, адміністратор), та має повну Swagger-документацію.

🚀 Основні можливості
🔐 Аутентифікація та авторизація

Реалізована через JWT-токени (access & refresh).

Три ролі користувачів:

👤 User — звичайний користувач.

🛡️ Moderator — може видаляти коментарі, переглядати/видаляти рейтинги.

⚙️ Admin — повний доступ до CRUD-операцій з користувачами, фото, коментарями.

Перший зареєстрований користувач автоматично отримує роль Admin.

Адміністратор може банити користувачів (робити неактивними).

🖼️ Робота зі світлинами

📤 Завантаження фото з описом (POST).

✏️ Редагування опису фото (PUT).

🗑️ Видалення фото (DELETE).

🔗 Отримання фото за унікальним посиланням (GET).

🏷️ До 5 тегів на одне фото (створюються або використовуються повторно).

☁️ Інтеграція з Cloudinary для трансформацій (обрізання, фільтри, розмір).

🔗 Генерація унікального URL та QR-коду для трансформованих фото.

🔒 CRUD-операції для адміністраторів над усіма фото користувачів.

💬 Коментування

Користувачі можуть залишати коментарі під фото.

Автор коментаря може редагувати, але не видаляти свій коментар.

Модератори й адміністратори можуть видаляти коментарі.

Для коментарів зберігаються поля:

created_at — час створення

updated_at — час останнього редагування

⭐ Рейтинг

Користувач може оцінити фото від 1 до 5 зірок.

Рейтинг = середнє значення усіх оцінок.

Кожен користувач може оцінити фото лише один раз.

Неможливо оцінювати власні фото.

Модератори та адміністратори можуть переглядати і видаляти оцінки.

🔍 Пошук і фільтрація

Пошук світлин за:

🔑 ключовим словом (опис)

🏷️ тегом

Можна фільтрувати результати за:

⭐ рейтингом

📅 датою додавання

Адміністратори можуть здійснювати пошук за користувачами.

👤 Профіль користувача

Публічна сторінка користувача за username з:

ім’ям

датою реєстрації

кількістю завантажених фото

Окремий маршрут для редагування власної інформації.

Адміністратор може деактивувати користувача.

🚪 Logout і чорний список токенів

Реалізований механізм logout, при якому access token додається до чорного списку на час своєї дії через Redis.

🗂️ Структура проєкту
Project-Python-Web/
│
├── Dockerfile                # Інструкції для створення Docker-образу
├── docker-compose.yml        # Конфігурація сервісів (FastAPI, PostgreSQL, Redis)
├── README.md                 # Поточний файл
├── alembic/                  # Міграції бази даних
│   ├── env.py
│   └── versions/
│
├── app/
│   ├── main.py               # Точка входу застосунку FastAPI
│   ├── api/routers/          # REST-маршрути
│   ├── core/                 # Конфігурація, безпека
│   │   ├── config.py
│   │   └── security.py
│   ├── crud/                 # CRUD-операції для моделей
│   ├── db/                   # Підключення до бази (SQLAlchemy, Session)
│   ├── docs/                 # Swagger-описи, докстрінги
│   ├── models/               # SQLAlchemy ORM моделі
│   ├── schemas/              # Pydantic-схеми
│   ├── services/             # Логіка Cloudinary, QR, Redis, Auth
│   ├── utils/                # Допоміжні утиліти (пошта, slugify)
│   ├── deps.py               # Депенденсі для FastAPI
│
├── docs/                     # Технічна документація (Sphinx)
│
├── media/qrcodes/            # Збережені QR-коди
│
├── scripts/create_admin.py   # Скрипт для створення першого адміністратора
│
├── tests/                    # Тести (>90% покриття)
│   ├── test_auth.py
│   ├── test_comments.py
│   ├── test_photos.py
│   ├── test_ratings.py
│   ├── test_search.py
│   ├── functional/
│   └── unit/
│
├── pyproject.toml            # Конфігурація проєкту (Poetry)
├── requirements.txt          # Залежності
├── pytest.ini                # Налаштування Pytest
└── alembic.ini               # Конфігурація Alembic

⚙️ Технологічний стек
Компонент	Опис
FastAPI	Фреймворк для REST API
SQLAlchemy ORM	Робота з PostgreSQL
Alembic	Міграції бази даних
JWT (PyJWT)	Аутентифікація
Cloudinary	Хостинг та трансформації зображень
Redis	Зберігання чорного списку токенів
qrcode	Генерація QR-кодів
Docker / Docker Compose	Контейнеризація
pytest	Модульне тестування
Sphinx / Swagger UI	Документація API
🛠️ Встановлення та запуск
1️⃣ Клонування репозиторію
git clone https://github.com/<your-username>/PhotoShare.git
cd PhotoShare

2️⃣ Налаштування змінних середовища

Створи .env файл у корені:

DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/photoshare
SECRET_KEY=your_secret_key
ALGORITHM=HS256
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
REDIS_URL=redis://redis:6379

3️⃣ Запуск через Docker Compose
docker-compose up --build

4️⃣ Міграції бази даних
docker-compose exec web alembic upgrade head

5️⃣ Створення адміністратора
docker-compose exec web python scripts/create_admin.py

🌐 Документація API

Після запуску відкрий Swagger UI:
👉 http://localhost:8000/docs

або ReDoc:
👉 http://localhost:8000/redoc

🧪 Тестування

Запуск тестів:

pytest --cov=app --cov-report=term-missing


Очікуване покриття: >90%

☁️ Деплой

Застосунок можна деплоїти на:

Koyeb

Fly.io

Render.com

Після деплою Swagger буде доступний за /docs.
 
### Як запустити додаток:

Створюємо віртуальне середовище.

python3 -m venv .venv
source .venv/bin/activate   # для Linux / Mac

# або

.venv\Scripts\activate   # для Windows PowerShell

Встановлюємо бібліотеки
pip install -r requirements.txt

# Запусти контейнер
docker-compose down             --- для перезапуску видаляємо старі контейнера
docker-compose up -d --build
docker ps

docker-compose logs -f web
 

# Запусти локально

uvicorn app.main:app --reload

# Перезапусти тести
docker compose exec web pytest -q

Коли попрацювали і зробили якісь зміни і нам треба зробити PR то ми виконуємо крок покрокові:

1. git add .
2. git commit -m '...(Тут буде назва вашого коментаря)'
3. git push
4. git push origin (назва вашої гілки)
5. Переходимо на гілку девелопер
   git checkout developer
6. git merge --no-ff (назва вашої гілки) -m '...(Короткий опис PR)'
7. git push origin developer
8. Виходимо віртуального середовища за допомогою команди: deactivate
 